Alunos: Pedro Augusto Ramalho Duarte 170163717
		Wallif Cordeiro Bandeira 17011

    switch (op){
        case 0:
            /* Com mudanca de chave primaria */
            //Analisa a matricula de entrada
            do{
                system("clear");
                printf("Digite a matricula do aluno:");
                scanf("%d", &matricula);
                ref = existe_matricula(matricula, arquivo);
            }while(verifica_matricula(matricula) && ref != -1);
            fseek(arquivo, ref ,SEEK_SET);
            ler_linha_arquivo(arquivo, &reg);
            reg = get_matricula(reg, arquivo);
            reg = entrada_alterar(reg);
            fseek(arquivo, ref, SEEK_SET);
            escreve_arquivo(arquivo, &reg);
            fclose(arquivo);
            return;
        case 1:
            /* Sem mudanca de chave primaria */
            //Analisa a matricula de entrada
            do{
                system("clear");
                printf("Digite a matricula do aluno:");
                scanf("%d", &matricula);
                ref = existe_matricula(matricula, arquivo);
            }while(verifica_matricula(matricula) && ref != -1);
            fseek(arquivo, ref ,SEEK_SET);
            ler_linha_arquivo(arquivo, &reg);
            reg = entrada_alterar(reg);
            return;
        default:
            printf("ERRO\n");
    }